# Nginx配置文件 - 习惯日记小程序
# 用途：配置反向代理，将HTTP请求转发到后端Node.js服务

# HTTP服务器配置
server {
    listen 80;
    server_name your-domain.com;  # 请替换为您的域名
    
    # 访问日志和错误日志
    access_log /var/log/nginx/habit-diary-access.log;
    error_log /var/log/nginx/habit-diary-error.log;
    
    # 客户端上传文件大小限制
    client_max_body_size 10M;
    
    # API反向代理到后端Node.js服务
    location /api/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # WebSocket支持（如果需要）
    location /ws/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    
    # 健康检查端点
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
    
    # 重定向HTTP到HTTPS（SSL证书配置后启用）
    # return 301 https://$server_name$request_uri;
}

# HTTPS服务器配置（SSL证书配置后启用）
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;  # 请替换为您的域名
#     
#     # SSL证书配置
#     ssl_certificate /etc/ssl/certs/your-domain.com.crt;
#     ssl_certificate_key /etc/ssl/private/your-domain.com.key;
#     
#     # SSL优化配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     
#     # 访问日志和错误日志
#     access_log /var/log/nginx/habit-diary-ssl-access.log;
#     error_log /var/log/nginx/habit-diary-ssl-error.log;
#     
#     # 客户端上传文件大小限制
#     client_max_body_size 10M;
#     
#     # API反向代理到后端Node.js服务
#     location /api/ {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         
#         # 超时设置
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
#     
#     # WebSocket支持
#     location /ws/ {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
#     
#     # 健康检查端点
#     location /health {
#         access_log off;
#         return 200 "healthy\n";
#         add_header Content-Type text/plain;
#     }
# }
